<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="notice">
  	
  	<!-- 목록 조회 + 검색 + 페이징 -->
  	<select id="selectList" parameterType="map" resultType="com.exam.gagi.model.Notice">
  		SELECT *
  		FROM NOTICES
  		WHERE TITLE LIKE '%' || #{search} || '%'
  		ORDER BY ID DESC
  	</select>
  	
  	<!-- 총 게시글 수 조회 -->
  	<select id="selectCount" parameterType="string" resultType="int">
  		SELECT COUNT(*)
        FROM NOTICES
        WHERE TITLE LIKE '%' || #{search} || '%'
  	</select>
  	
  	<!-- 단일 조회 -->
  	<select id="selectPost"  parameterType="int" resultType="com.exam.gagi.model.Notice">
  		SELECT *
        FROM NOTICES
        WHERE ID = #{id}
  	</select>
  	
  	<!-- 공통 CRUD -->
  	<insert id="insert" parameterType="com.exam.gagi.model.Notice">
  		INSERT INTO NOTICES (title, content, visible, posted_at, expires_at, admin_id, created_at)
        VALUES (#{title}, #{content}, #{visible}, #{postedAt}, #{expiresAt}, #{adminId}, NOW())	
  	</insert>
  	
	<update id="update" parameterType="com.exam.gagi.model.Notice">
		UPDATE NOTICES
        SET title = #{title},
            content = #{content}
            visible = #{visible}
            posted_at = #{posted_at}
            expires_at = #{expires_at}
            admin_id = #{adminId}
            updated_at = NOW()
        WHERE id = #{id}
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE FROM NOTICES WHERE id = #{id}
	</delete>
  </mapper>
  