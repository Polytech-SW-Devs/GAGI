<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myboard">

	

	<select id="list" resultType="Myboard">
		SELECT M.*, C.NAME CATEGORY_NAME
		FROM MYBOARD M
		JOIN CATEGORIES C ON M.CATEGORY_ID = C.CATEGORY_ID
	</select>
	
	<insert id="add">
		INSERT INTO MYBOARD(BOARD_ID, CATEGORY_ID, TITLE, CONTENT)
		VALUES(BOARD_ID_SEQ.NEXTVAL, #{categoryId}, #{title}, #{description})
	</insert>


	<select id="categoryList" resultType="Categories">
		select category_id, name
		from CATEGORIES
	</select>

	
</mapper>