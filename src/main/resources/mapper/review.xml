<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="review">
  	
  	<!-- 목록 조회 + 검색 + 페이징 -->
  	<select id="selectList" parameterType="map" resultType="com.exam.gagi.Review">
  		SELECT *
        FROM REVIEWS
        WHERE COMMENT LIKE CONCAT('%', #{search}, '%')
        ORDER BY ID DESC
        LIMIT #{offset}, #{size}
  	</select>
  	
  	<!-- 총 게시글 수 조회 -->
  	<select id="selectCount" parameterType="string" resultType="int">
  		SELECT COUNT(*)
        FROM REVIEWS
        WHERE COMMENT LIKE CONCAT('%', #{search}, '%')
  	</select>
  	
  	<!-- 단일 조회 -->
  	<select id="selectPost"  parameterType="int" resultType="com.exam.gagi.Review">
  		SELECT *
        FROM REVIEWS
        WHERE ID = #{id}
  	</select>
  	
  	<!-- 공통 CRUD -->
  	<insert id="insert" parameterType="com.exam.gagi.Review">
  		INSERT INTO REVIEWS (order_item_id, reviewer_id, target_id, rating, comment, created_at)
        VALUES (#{orderItemId}, #{reviewerId}, #{targetId}, #{rating}, #{comment}, NOW())	
  	</insert>
  	
	<update id="update" parameterType="com.exam.gagi.Review">
		UPDATE REVIEWS
        SET rating = #{rating},
            comment = #{comment}
        WHERE id = #{id}
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE FROM REVIEWS WHERE id = #{id}
	</delete>
  </mapper>
  