<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="review">
  	<select id="selectList" parameterType="map" resultType="com.exam.gagi.Review">
  		SELECT *
        FROM REVIEWS
        WHERE TITLE LIKE CONCAT('%', #{search}, '%')
        ORDER BY CREATED_AT DESC
        LIMIT #{offset}, #{size}
  	</select>
  	
  	<select id="selectReview"  parameterType="int" resultType="com.exam.gagi.Review">
  		SELECT *
        FROM REVIEWS
        WHERE ID = #{id}
  	</select>
  	
  	<select id="selectCount" parameterType="string" resultType="int">
  		SELECT COUNT(*)
        FROM REVIEWS
        WHERE TITLE LIKE CONCAT('%', #{search}, '%')
  	</select>

  </mapper>
  