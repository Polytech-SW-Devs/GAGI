<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="inquiry">
  	<select id="selectList" parameterType="map" resultType="com.exam.gagi.Inquiry">
  		SELECT *
        FROM INQUIRIES
        WHERE TITLE LIKE CONCAT('%', #{search}, '%')
        ORDER BY CREATED_AT DESC
        LIMIT #{offset}, #{size}
  	</select>
  	
  	<select id="selectInquiry"  parameterType="int" resultType="com.exam.gagi.Inquiry">
  		SELECT *
        FROM INQUIRIES
        WHERE ID = #{id}
  	</select>
  	
  	<select id="selectCount" parameterType="string" resultType="int">
  		SELECT COUNT(*)
        FROM INQUIRIES
        WHERE TITLE LIKE CONCAT('%', #{search}, '%')
  	</select>

  </mapper>
  