<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="inquiry">
    <!-- 목록 조회 + 검색 + 페이징 -->
  	<select id="selectList" parameterType="map" resultType="com.exam.gagi.model.Inquiry">
  		SELECT *
        FROM INQUIRIES
        WHERE SUBJECT LIKE '%' || #{search} || '%'
           OR MESSAGE LIKE '%' || #{search} || '%'
        ORDER BY CREATED_AT DESC
  	</select>
  	
  	<!-- 총 게시글 수 조회 -->
  	<select id="selectCount" parameterType="string" resultType="int">
  		SELECT COUNT(*)
        FROM INQUIRIES
        WHERE SUBJECT LIKE '%' || #{search} || '%'
           OR MESSAGE LIKE '%' || #{search} || '%'
  	</select>
  	
  	<!-- 단일 조회 -->
  	<select id="selectInquiry"  parameterType="int" resultType="com.exam.gagi.model.Inquiry">
  		SELECT *
        FROM INQUIRIES
        WHERE ID = #{id}
  	</select>
  	
	<!-- 공통 CRUD -->
	<insert id="insert" parameterType="com.exam.gagi.model.Inquiry">
  		INSERT INTO INQUIRIES (user_id, responder_id, category, subject, message, status, created_at)
        VALUES (#{userId}, #{responderId}, #{category}, #{subject}, #{message}, #{status}, NOW())	
  	</insert>
  	
	<update id="update" parameterType="com.exam.gagi.model.Inquiry">
		UPDATE INQUIRIES
        SET responder_id = #{responderId},
            category = #{category},
            subject = #{subject},
            message = #{message},
            status = #{status},
            created_at = NOW()
        WHERE id = #{id}
	</update>
	
	<delete id="delete" parameterType="int">
		DELETE FROM INQUIRIES WHERE id = #{id}
	</delete>
  </mapper>
  