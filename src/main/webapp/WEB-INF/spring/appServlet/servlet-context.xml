<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="
        http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	<resources mapping="/images/**" location="/images/" />
	
	<!-- url 변경 및 저장폴터 변경하는 설정(js라는 폴더안으로 javascript파일을 넣고, url: js/**으로 표시됨) -->
	<resources location="/js/" mapping="js/**"></resources> 
	
	<!-- 파일업로드 경로 -->
	<resources location="file:///d:/upload/" mapping="/upload/**"></resources>
	
	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<context:component-scan base-package="com.exam.gagi" />
	    <!-- 💤 인터셉터 전체 비활성화 -->
    <!--
    <interceptors>
        <interceptor>
            <mapping path="/mypage/**"/>
            <mapping path="/mycart/**"/>
            <mapping path="/product/add"/>
            <mapping path="/inquiry/write"/>
            <mapping path="/report/write"/>
            <mapping path="/report/writeWithFile"/>
            <mapping path="/review/write"/>
            <mapping path="/reply/add"/>
            <mapping path="/product/update"/>
            <mapping path="/inquiry/edit/**"/>
            <mapping path="/report/edit/**"/>
            <mapping path="/review/edit/**"/>
            <mapping path="/reply/delete/**"/>
            <mapping path="/logout"/>
            <mapping path="/report/user/**"/>
            <mapping path="/inquiry/user/**"/>
            <beans:bean class="com.exam.gagi.interceptor.LoginInterceptor" />
        </interceptor>

        <interceptor>
            <mapping path="/report/admin/**"/>
            <mapping path="/notice/write"/>
            <mapping path="/notice/edit/**"/>
            <mapping path="/notice/delete/**"/>
            <beans:bean class="com.exam.gagi.interceptor.AdminInterceptor" />
        </interceptor>
    </interceptors>
    -->
	 <tx:annotation-driven/>
</beans:beans>
